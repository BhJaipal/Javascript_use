import * as React from "react";
import $ from "./jquery.js";

export default class Sphere{
    calculate() {
        let out= `Total Surface Area: ${sp.TotalSurfaceArea()}\nLateral Surface Area: ${cuboid.LateralSurfaceArea()}\nVolume: ${cuboid.Volume()}`;
    }
}